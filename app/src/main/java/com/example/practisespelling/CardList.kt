package com.example.practisespelling

class CardList (private val cardList: MutableList<TextCard>) {

    // Tar in en listOfCards.
    // Tar in en index int som plussas på i main.
    /*
   fun newCard(index: Int): TextCard? {
       val newCard: TextCard
       newCard = cardList[index]

       return newCard
   }

     */
}